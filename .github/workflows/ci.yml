name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate-plan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate repository plan
        run: node scripts/ci-check.js
